@model List<blog_site.Models.ViewModels.PostVM>
<div id="intro" class="p-5 text-center bg-light align-items-center justify-content-center d-flex flex-column">
    <h1 class="mb-3 h2">Ana Başlık</h1>
    <p class="mb-3 w-75 text-center">Bu sitenin oluşturulma amacı tasarım geliştirmek değil, halihazırda
        oluşturulmuş
        bir projeyi .NET MVC Projesi haline getirmek ve veritabanından veri çekip burada göstermektir.</p>
    <a class="btn btn-primary m-2" href="/posts/">Tüm
        Gönderileri Görüntüle</a>
</div>

<section class="text-center">
    <h4 class="mb-5"><strong>Son Gönderiler</strong></h4>

    <div class="row">
        @foreach (var post in Model)
        {
            <div class="col-lg-4 col-md-12 mb-4">
                <div
                    class="card @(post.turId == 1 ? "tech-card" : post.turId == 2 ? "health-card" : post.turId == 3 ? "travel-card" : post.turId == 4 ? "education-card" : post.turId == 5 ? "culture-card" : "")  h-100">
                    <div class="bg-image hover-overlay" data-mdb-ripple-init data-mdb-ripple-color="light">
                        <a href="/post/@post.id">
                            <div
                                class="cardimg @(post.turId == 1 ? "tech" : post.turId == 2 ? "health" : post.turId == 3 ? "travel" : post.turId == 4 ? "education" : post.turId == 5 ? "culture" : "")">
                                <h4 class="p-5">@post.turAdi</h4>
                            </div>
                        </a>
                    </div>
                    <div class="card-body">
                        <h5 class="card-title text-start">@post.baslik</h5>
                        <p class="text-start">
                            @post.onsoz
                        </p>
                    </div>
                </div>
            </div>
        }
    </div>
</section>